{
  "name": "Rate limit AI routes",
  "description": "URecruitment #93: at most 60 requests per 60 s per IP to /api/ai/*. Excess gets 429. Bounds speed only; the monthly spend cap bounds total cost.",
  "active": true,
  "conditionGroup": [
    {
      "conditions": [
        { "type": "path", "op": "pre", "value": "/api/ai/" }
      ]
    }
  ],
  "action": {
    "mitigate": {
      "action": "rate_limit",
      "rateLimit": {
        "algo": "fixed_window",
        "window": 60,
        "limit": 60,
        "keys": ["ip"],
        "action": "rate_limit"
      },
      "actionDuration": null
    }
  }
}
